# Comment
#-
    This is a
    multi line comment
-#

local var = 1
global var = if true {
    10
} else {
    5
}
print(var)

local add = |a, b| a + b
print(add(2, 3)) # 5

component StyledButton (bg_color?) {
    Object "id" {
        # Simple default value pattern
        background = bg_color || "#ffffff"
    }
}

component LooseStyledButton (any: test) {
    # ?. makes it null if it even if doesn't exist
    local bg_color = test?.bg_color

    Object component.id {
        # Simple default value pattern
        background = bg_color || "#ffffff"

        component.children
    }
}

fn show() {
    StyledButton {
        bg_color = "#000000"
    }
}

LooseStyledButton "test" {
    show()
}