# Single line comment
#-
    Multi 
    line 
    comment
-#

import "other.nbl" as other

local example = 4 + 6 * 2
print(example)

local list = [1, 2, 3]
local map = { key: value, key2: value }

fn fn_example(pram1: String) {
    print(pram1)
}

fn_example("Hello, World!")

Object "example_node" {
    value = 10
}